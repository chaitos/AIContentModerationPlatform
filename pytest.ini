[pytest]
asyncio_mode = auto
testpaths = tests
env =
    POSTGRES_HOST=localhost
    POSTGRES_USER=moderator
    POSTGRES_PASSWORD=secret
    POSTGRES_DB=moderator_test
    REDIS_HOST=localhost
    SECRET_KEY=test-secret-key
    OPENAI_API_KEY=test-key
